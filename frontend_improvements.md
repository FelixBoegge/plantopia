- [x] I want to keep a green style so light mode should have a light green color palette (sage, eucalyptus, etc...) and dark mode should have a very dark green color palette
- [x] everything clickable should get highligeted or atleast the mouse should turn into a head to give the user feedback, that this is a clicable item when hovering
- [x] registration and password reset both tell me to check an inbox that never receives anything. Both screens say a message "is on its way" unconditionally, and that is false whenever no mail provider is configured: the backend falls back to writing the link into the API log instead of sending it (`core/mail.py`, `ConsoleMailer`). A reviewer running the app locally has the link sitting in their own terminal and no reason to look there. Real delivery needs `PLANTOPIA_RESEND_API_KEY` and a verified sender domain, which will not exist before the review — so the screens themselves have to stop claiming an email was sent and say where the link actually went. Registration is `Register.tsx`, reset is `ResetPassword.tsx`; identical wording, identical fix. Whatever the screens say must not depend on whether the address exists — both endpoints deliberately answer the same for a known and an unknown address, and the copy must not undo that.
- [x] when registration is refused, the reason is shown at the top of the form rather than on the field that caused it. Checked in the browser: submitting without ticking the privacy box does produce a red "the privacy notice must be agreed to", so the message is not missing — but it sits above the email field while the checkbox that caused it is near the bottom of a long privacy notice, and that checkbox gets no error styling of its own. The wording is the server's lowercase sentence rather than copy written for a person. Put the message next to what it is about. The two refusals worth handling are the privacy box and a password under 12 characters.
- [x] The header when you start a diagnosis should be called "Diagnose a plant" not "Check a plant". (`Wizard.tsx` says "Check a plant"; every link to it — the header nav and the button on the plants list — says "Diagnose a plant".)
- [x] The image upload field should be more distinct. It should be a clear button called "upload images" and after uplaoding cards with the files name should appear to show the user that those images where uploaded
- [x] When the user does not provide his guess/ name of a plant, there should only be 2 options to select from during the interrupt: the vision models suggestion and plantnets suggestion. I just ran a diagnosis and I got 3 options: "Your photo and a plant database agree, very confident", "Matched against a plant database, little more than a guess", "Matched against a plant database, little more than a guess". I thought I would get those options: vision models suggestion, plantnets suggestions, optional users input.
- [x] date should be a calender picker during the interrupt
- [x] location and date should be listed right after the plant identification part in the interrupt
- [ ] What Plantopia is doing should appear in a sidebar on the left and it should be more specific, like what exaclty is used when running a vision model or calling plantnet or meteo weather...
- [x] the app called a diagnosis run a "check" in some places and a "diagnosis" in others. One word for one thing: "diagnosis"/"diagnose" everywhere in UI copy, buttons, allowance counts, error details and step descriptions. The other senses of "check" (check your email, checking your link, checking the photographs, checkbox) are untouched.
